name: Test App
on: push
jobs:
  test:
    run-on: ubuntu-latest
    steps:
      - name: Get the repository code
        uses: actions/checkout@v4
      - name: Install dependecies
        run: npm ci
      - name: Test
        run: npm run test